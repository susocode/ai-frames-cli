{
  "name": "ai-frames",
  "version": "0.1.2",
  "description": "AI context manager for multi-repo workspaces",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/ui"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/ui\"",
    "build": "npm run build -w packages/server && npm run build -w packages/ui",
    "lint": "npm run lint -w packages/server && npm run lint -w packages/ui"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
